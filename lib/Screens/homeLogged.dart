import 'package:flutter/material.dart';
class HomeLogged extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Homepage after logged in'),
      ),
    );
  }
}